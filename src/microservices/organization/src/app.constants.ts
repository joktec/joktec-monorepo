export const enum Environment {
  DEVELOPMENT = 'development',
  STAGING = 'staging',
  PRODUCTION = 'production',
}

export const enum RedisCacheKey {
  ORGANIZATION_PACKAGE_MISC = 'organization_package_misc',
  ORGANIZATION_SIZE_MISC = 'organization_size_misc',
}
