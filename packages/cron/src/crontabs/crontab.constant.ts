export enum CrontabType {
  CRON = 'cron',
}

export enum CrontabStatus {
  ACTIVATED = 'activated',
  DISABLED = 'disabled',
}

export enum CrontabHistoryType {
  AUTOMATIC = 'automatic',
  MANUAL = 'manual',
}

export enum CrontabHistoryStatus {
  COMPLETED = 'completed',
  FAILED = 'failed',
}
