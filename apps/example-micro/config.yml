$schema: ../../tools/types/config.schema.json

micro:
  port: 8010
  microservices:
    tcp:
      host: 0.0.0.0
      port: 8010

log:
  level: info
  useFilter: false
  format: pretty
  logStash:
    host: 51.222.44.17
    port: 31658
    enable: false

mysql:
  host: localhost
  port: 3306
  username: root
  password: root
  database: sample_db

http:
  timeout: 18000000
