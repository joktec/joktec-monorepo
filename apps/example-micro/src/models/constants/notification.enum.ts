export enum NotificationType {
  DEFAULT = 'default',
  ARTICLE = 'article',
  COMMENT = 'comment',
  EMOTION = 'emotion',
  NEWS = 'news',
  BROWSER = 'browser',
}

export enum NotificationStatus {
  UNPUBLISHED = 'unpublished',
  ACTIVATED = 'activated',
  DISABLED = 'disabled',
  EXPIRED = 'expired',
}
