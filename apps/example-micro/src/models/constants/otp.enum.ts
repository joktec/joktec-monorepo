export enum OTPType {
  REGISTER = 'register',
  RESET = 'reset',
}

export enum OTPStatus {
  ACTIVATED = 'activated',
  VERIFIED = 'verified',
  SUCCESS = 'success',
  EXPIRED = 'expired',
  DISABLED = 'disabled',
}
