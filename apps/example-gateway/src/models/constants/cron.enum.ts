export enum CronType {
  CRON = 'cron',
}

export enum CronStatus {
  ACTIVATED = 'activated',
  DISABLED = 'disabled',
}

export enum CronHistoryType {
  AUTOMATIC = 'automatic',
  MANUAL = 'manual',
}

export enum CronHistoryStatus {
  COMPLETED = 'completed',
  FAILED = 'failed',
}
