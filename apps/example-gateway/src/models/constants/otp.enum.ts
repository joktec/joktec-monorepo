export enum OTPType {
  LOGIN = 'login',
  REGISTER = 'register',
  RESET = 'reset',
  EMAIL = 'email',
}

export enum OTPStatus {
  ACTIVATED = 'activated',
  VERIFIED = 'verified',
  SUCCESS = 'success',
  EXPIRED = 'expired',
  DISABLED = 'disabled',
}
