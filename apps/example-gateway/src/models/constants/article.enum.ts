export enum ArticleType {
  DEFAULT = 'default',
  FEED = 'feed',
  CARD = 'card',
}

export enum ArticleResource {
  DEFAULT = 'default',
  YOUTUBE = 'youtube',
  INSTAGRAM = 'instagram',
}

export enum ArticleStatus {
  IN_REVIEW = 'in_review',
  ACTIVATED = 'activated',
  DISABLED = 'disabled',
}

export enum ArticleFileType {
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum ArticleFileStatus {
  ACTIVATED = 'activated',
  DISABLED = 'disabled',
}
